PySide6>=6.6
opencv-python>=4.8
numpy>=1.24
librosa>=0.10
soundfile>=0.12
sounddevice>=0.4   # live preview audio (needs system portaudio)
pygame>=2.5        # gamepad input (live control)
python-rtmidi>=1.5 # MIDI input (live control)
mss>=9.0           # screen capture source
# ffmpeg + ffprobe must be on PATH (system packages, not pip)
# gamepad also needs the user in the `input` group
moderngl>=5.8     # GPU shader effects (Phase 2)
pyvirtualcam>=0.10  # virtual cam output (Phase 4, needs v4l2loopback)
